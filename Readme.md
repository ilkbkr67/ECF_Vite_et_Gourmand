# Intro

- Ce Readme a pour But d'expliquer la procedure d'installation du projet. 

# Installation

Le projet utilise Symfony donc il faut tout les prérequis c'est a dire avoir : 

    - PHP 8.4 minimum
    - Composer

Verifier avec la commande si tout est ok

    - symfony check:requirements

Une fois les prérequis installé lancé la commande

    - symfony new my_project_directory --version="7.4.x" --webapp"

Nota : Dans la version 8.0 de symfony le webbpack encore n'est pas fonctionnelle. 